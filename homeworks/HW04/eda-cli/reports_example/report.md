# EDA-отчёт для example.csv

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчёта

- max_hist_columns = **6**
- top_k_categories = **10**
- min_missing_share = **30%**

## Качество данных (эвристики)

- Оценка качества: **0.54**
- Макс. доля пропусков: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Колонки с пропусками выше порога (30%):
  - нет

- Константные колонки: **False**
- Высокая кардинальность категорий: **False**
- Дубликаты идентификаторов: **True**
- Слишком много нулей: **True**
- Колонок с пропусками >= порога: **0**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные колонки

См. файлы в `top_categories/`.

## Гистограммы

См. файлы `hist_*.png`.
